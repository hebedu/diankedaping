# 大屏拓扑配置页交互规则说明 · 最终版

> 适用对象：大屏拓扑配置编辑器 / 拓扑编排工作台  
> 最终模型：**节点 + 区域 + 连线**  
> 版本定位：用于产品设计、前端原型、Antigravity 生成、交互验收

---

## 1. 页面定位

### 1.1 页面名称

**大屏拓扑配置**

### 1.2 页面性质

该页面是一个**拓扑配置工作台**，不是普通的大屏查看页。

用户进入页面后，默认处于**可编辑状态**，不需要额外点击“编辑”按钮。所有新增、编辑、删除、拖拽、连线等操作都只影响当前草稿，不直接影响线上正式大屏；只有点击“发布配置”后，当前草稿才会覆盖正式大屏配置。

---

## 2. 最终核心模型

```text
拓扑 = 区域 + 节点 + 连线

节点 = 显示名称 + 图标类型 + 子拓扑关联 + 坐标 + 所属区域 

子拓扑关联 = 必选
- 关联子拓扑：子拓扑入口节点，可从该节点进入已有子拓扑（仅限大屏展示子拓扑）
```

### 2.1 概念统一规则


| 旧概念      | 最终处理方式                                       |
| -------- | -------------------------------------------- |
| 设备       | 不再作为新增节点时可直接选择的关联对象；仅作为“子拓扑下的关联设备明细”在节点详情中展示 |
| 集群 / 设备组 | 不再作为独立资源概念，改为节点的一种图标或展示类型                    |
| 添加设备     | 改为“新增节点”，节点图标可选择防火墙、路由器、交换机、服务器等             |
| 添加集群     | 改为“新增节点”，图标选择“群组 / 集群”                       |
| 关联设备     | 新增 / 编辑节点中删除该选项，不再支持节点直接关联物理设备               |
| 关联子拓扑    | 只能在节点内配置，不能把子拓扑作为独立对象添加到画布                   |
| 进入内部拓扑   | 改为从“已关联子拓扑”的节点进入                             |


### 2.2 最终产品原则

```text
节点是画布中的唯一核心对象。
设备不是节点新增时的直接关联项。
集群不是独立资源，只是节点的一种图标或语义展示。
子拓扑不能单独添加到画布，只能通过节点关联已有拓扑图。
已关联子拓扑的节点详情中，应展示该子拓扑下的关联设备列表。
区域只是视觉分组，不是强业务容器。
连线只表达节点之间存在关系。
```

## 3. 页面整体结构

页面保留当前三段式结构：

```text
┌──────────────────────────────────────────────┐
│ 顶部工具栏：状态 / 撤销 / 重做 / 新增区域 /  预览 / 保存 / 发布 /版本历史 │
├──────────────┬──────────────────────┬──────────────┤
│ 左侧节点管理   │      拓扑画布区        │ 右侧属性面板   │
│              │ 区域 / 节点 / 连线      │ 动态详情       │
└──────────────┴──────────────────────┴──────────────┘
```


| 区域     | 作用                     |
| ------ | ---------------------- |
| 顶部工具栏  | 全局编辑操作、状态提示、版本历史、保存和发布 |
| 左侧节点管理 | 管理当前拓扑中的节点，不再作为资源库     |
| 中间拓扑画布 | 展示和编辑区域、节点、连线          |
| 右侧属性面板 | 根据当前选中对象动态切换详情         |


右侧属性面板切换规则：


| 选中对象 | 右侧面板    |
| ---- | ------- |
| 未选中  | 拓扑概览    |
| 选中节点 | 节点详情    |
| 选中区域 | 不展示区域详情 |
| 选中连线 | 连线详情弹窗  |


---

## 4. 页面状态规则

### 4.1 默认模式

页面默认进入：**编辑模式**。

不需要额外点击“编辑”。

### 4.2 页面加载规则


| 场景    | 加载内容                    |
| ----- | ----------------------- |
| 已存在草稿 | 默认加载最近一次保存的草稿           |
| 不存在草稿 | 加载当前已发布版本，并作为新的编辑基础     |
| 加载失败  | 展示错误状态：拓扑配置加载失败，请刷新页面重试 |


### 4.3 未保存状态触发条件

以下操作均触发“有未保存修改”：

- 新增节点；
- 编辑节点；
- 删除节点；
- 拖拽节点；
- 新增区域；
- 编辑区域；
- 删除区域；
- 移动区域；
- 调整区域大小；
- 新增连线；
- 删除连线；
- 修改节点关联子拓扑；
- 修改节点所属区域；
- 从历史版本恢复为当前草稿。

## 5. 顶部工具栏规则

### 5.1 顶部按钮结构

```text
大屏拓扑配置  [有未保存修改]

撤销  重做  ｜  新增区域   ｜  预览大屏  保存草稿  发布配置 版本历史
```

### 5.2 按钮说明


| 按钮   | 类型   | 规则                      |
| ---- | ---- | ----------------------- |
| 撤销   | 编辑辅助 | 无可撤销操作时置灰               |
| 重做   | 编辑辅助 | 无可重做操作时置灰               |
| 新增区域 | 配置操作 | 打开新增区域弹窗                |
| 预览大屏 | 结果查看 | 进入预览模式                  |
| 保存草稿 | 次级操作 | 保存当前草稿，不影响线上            |
| 发布配置 | 主操作  | 二次确认后发布到正式大屏            |
| 版本历史 | 历史记录 | 从右侧打开版本历史抽屉，查看草稿版本和发布版本 |


## 6. 左侧节点管理面板

### 6.1 面板定位

左侧为：**节点管理**。

作用：

1. 新增节点；
2. 搜索当前拓扑中的节点；
3. 查看当前拓扑已有节点；
4. 快速定位节点；
5. 查看节点是否已关联子拓扑；
6. 对节点进行编辑、删除、定位。

左侧面板不再承担“设备库 / 集群库 / 资源拖拽库”的职责，也不再展示“关联设备”的入口。

### 6.2 面板结构

```text
节点管理

[+ 新增节点]

[搜索节点名称]

当前拓扑节点 5

[节点卡片]
[节点卡片]
[节点卡片]
```

如后续节点数量较多，可增加轻量筛选，但第一版不强制增加复杂筛选。

```

### 6.3 新增节点按钮

| 操作 | 规则 |
|---|---|
| 点击“新增节点” | 打开新增节点弹窗 |
| 新增成功 | 节点出现在画布中，并同步出现在左侧列表 |
| 默认位置 | 如果选择所属区域，放入该区域；否则放在当前画布视口中心 |
| 状态变化 | 页面变为“有未保存修改” |

### 6.4 搜索规则

搜索字段包括：

- 节点显示名称；



### 6.6 节点卡片展示

节点卡片建议展示：

```text
[图标] 节点显示名称
```

空间有限，卡片中可以只展示节点名称，子拓扑通过节点详情表达。

### 6.7 节点卡片交互


| 操作         | 行为                |
| ---------- | ----------------- |
| 单击节点卡片     | 选中画布节点，并将画布定位到该节点 |
| hover 节点卡片 | 显示定位、编辑、删除        |
| 点击定位       | 画布平滑移动到节点，并高亮     |
| 点击编辑       | 打开编辑节点弹窗          |
| 点击删除       | 弹出删除确认弹窗          |


### 6.8 空状态

无节点时：

```text
暂无节点
点击“新增节点”创建拓扑节点
```

搜索无结果时：

```text
未找到匹配节点
请尝试更换关键词
```

## 7. 新增 / 编辑节点规则

### 7.1 弹窗标题


| 场景  | 标题   |
| --- | ---- |
| 新增  | 新增节点 |
| 编辑  | 编辑节点 |


### 7.2 字段结构


| 字段    | 是否必填 | 说明                      |
| ----- | ---- | ----------------------- |
| 节点图标  | 必填   | 仅影响画布视觉表达，不代表创建真实设备     |
| 显示名称  | 必填   | 画布、左侧列表、右侧详情展示名称        |
| 关联子拓扑 | 必选   | 只能选择已有子拓扑，或在选择弹窗内新增后再关联 |


节点图标可以选择：防火墙、路由器、交换机、服务器、终端、群组 / 集群。

图标只是视觉表达，不代表创建真实设备，也不触发设备资源绑定。

### 7.3 关联子拓扑规则

节点只支持配置一种可进入的关联内容：**关联子拓扑**。

- 子拓扑只能从节点中关联，不能单独添加到画布；
- 一个节点最多关联一个子拓扑；
- 选择子拓扑后，大屏模式下可通过该节点进入目标子拓扑；
- 支持“查看子拓扑”按钮新开页进入网络拓扑tab页并查看该子拓扑视图。
- 支持新建子拓扑新开页跳转至网络拓扑页默认打开新建网络拓扑弹窗页

### 7.4 子拓扑选择弹窗

触发方式：在新增 / 编辑节点弹窗中点击“选择关联子拓扑”。

弹窗内容：

```text
选择关联子拓扑
请选择一个目标子拓扑

[搜索子拓扑名称]

[子拓扑列表]
  子拓扑名称
  设备数量 / 查看子拓扑

[取消] [确定关联]
```

规则：


| 项目    | 规则                             |
| ----- | ------------------------------ |
| 选择方式  | 单选，一个节点只能关联一个子拓扑               |
| 搜索    | 支持按子拓扑名称搜索                     |
| 新增子拓扑 | 可以在选择弹窗内新增子拓扑；新增后进行刷新展示最新子拓扑列表 |
| 未选择   | 不支持未选择                         |


提示文案建议：

```text
一个节点只能关联一个子拓扑作为下钻入口。
```

### 7.5 字段动态变化

```text
已选择关联子拓扑：
展示：节点图标、显示名称、关联子拓扑
保存后：节点成为子拓扑入口节点
```

### 7.6 新增节点流程

```text
点击左侧“新增节点”
打开新增节点弹窗
选择节点图标
填写显示名称
必选：选择关联子拓扑
点击保存
节点生成到画布
左侧列表同步新增
页面状态变为“有未保存修改”
```

### 7.7 编辑节点流程

```text
选中节点
点击右侧“编辑节点”或左侧卡片编辑按钮
打开编辑节点弹窗
修改节点图标、显示名称、关联子拓扑
点击保存
更新画布节点与左侧列表
页面状态变为“有未保存修改”
```

### 7.8 删除节点规则

点击删除节点时，必须二次确认。

弹窗文案：

```text
删除节点后，与该节点相关的连线也会同步删除。是否确认删除？
```

确认后：

- 删除节点；
- 删除与该节点相关的所有连线；
- 如果该节点关联了子拓扑，仅解除入口关系，不删除目标子拓扑；
- 页面状态变为“有未保存修改”。

## 8. 画布节点展示规则

### 8.1 节点基础展示

节点卡片由以下内容组成：

```text
图标
显示名称
```

### 8.2 节点

展示内容：

- 节点图标；
- 显示名称；

### 8.4 Hover 展示

节点 hover 时建议展示轻量信息，不要做大面积信息浮层。

节点 hover：

```text
节点名称
子拓扑入口
关联设备：N 台
```

### 8.5 选中态

节点选中后：

- 外框高亮；
- 显示连接锚点；
- 右侧属性面板展示节点详情；

## 9. 节点画布交互

### 9.1 选中节点

用户可以通过以下方式选中节点：

- 点击画布节点；
- 点击左侧节点卡片；

选中后：

- 节点高亮描边；
- 左侧节点卡片同步高亮；
- 右侧显示节点详情；
- 可显示连接锚点。

### 9.2 拖拽节点


| 场景   | 规则           |
| ---- | ------------ |
| 编辑模式 | 支持拖拽         |
| 预览模式 | 禁止拖拽         |
| 拖入区域 | 可更新所属区域      |
| 拖出区域 | 可变为“未指定区域”   |
| 拖拽结束 | 记录坐标，进入未保存状态 |


### 9.3 节点点击行为


| 节点类型 | 编辑模式点击      | 预览模式点击 |
| ---- | ----------- | ------ |
| 普通节点 | 选中节点，右侧展示详情 |        |


建议第一版：

```text
编辑模式：点击只负责选中。
预览模式：不支持点击。
```

## 10. 右侧属性面板规则

### 10.1 选中节点：节点详情

节点详情需要从“资源绑定详情”改为“节点自身信息 + 子拓扑下关联设备”。


| 字段       | 说明              |
| -------- | --------------- |
| 节点名称     | 显示名称            |
| 节点图标类型   | 防火墙 / 路由器 / 群组等 |
| 所属区域     | 无则展示“不指定区域”     |
| 子拓扑下关联设备 | 仅当节点已关联子拓扑时展示   |


子拓扑下关联设备展示规则：

```text
标题：子拓扑下关联设备（N）
列表项：设备名称 / IP / 设备类型
加载失败：设备明细加载失败，请稍后重试
```

说明：

- 右侧详情不再展示设备状态、CPU、内存等运行态指标；
- 如果节点关联了子拓扑，详情区域重点展示该子拓扑下的关联设备列表；

按钮规则：


| 节点状态 | 按钮          |
| ---- | ----------- |
| 普通节点 | 编辑节点 / 删除节点 |


## 11. 区域规则

### 11.1 区域定位

区域是画布中的**视觉分组**，用于表达拓扑分区，例如：外部用户区、核心网络安全域、算力服务区、数据服务区、运维区。

区域不强制绑定节点，节点可以在区域内、区域外、从区域内拖出、从区域外拖入；区域归属只是用于展示和辅助管理，不应成为新增节点的强制条件。

### 11.2 区域展示


| 元素   | 规则           |
| ---- | ------------ |
| 区域名称 | 展示在区域框左上方    |
| 区域边框 | 轻量虚线或浅色实线    |
| 区域背景 | 低透明度浅色底      |
| 区域层级 | 始终在节点下方      |
| 区域操作 | hover 或选中时显示 |


### 11.3 区域操作

区域 hover 后可显示：

```text
编辑区域
删除区域
```


| 操作   | 行为       |
| ---- | -------- |
| 编辑区域 | 打开编辑区域弹窗 |
| 删除区域 | 弹出删除确认   |


### 11.4 区域尺寸规则

```text
区域大小由用户手动控制。
区域不会因为内部节点移动而自动变大。
区域不会因为节点拖出而自动变小。
```

### 11.5 删除区域规则


| 场景     | 规则                          |
| ------ | --------------------------- |
| 区域内无节点 | 删除区域 提示用户：确定要删除区域：区域名称 吗？   |
| 区域内有节点 | 提示用户：当前区域内存在节点，请先迁移或删除区域内对象 |
| 确认     | 不做变更                        |


---

## 12. 连线规则

### 12.1 连线定位

连线只表达：**节点与节点之间存在关系**。

连线不表达：方向、流量、链路状态、带宽、告警、入流量 / 出流量。

### 12.2 连线对象


| 对象      | 是否可连接 |
| ------- | ----- |
| 节点 → 节点 | 可以    |
| 节点 → 区域 | 不可以   |
| 区域 → 区域 | 不可以   |
| 节点 → 自己 | 不可以   |


### 12.3 创建连线

```text
节点 hover 显示连接锚点
点击或拖拽选择起点节点
选择终点节点
系统创建连线
页面状态变为“有未保存修改”
```

### 12.4 连线校验


| 校验项  | 规则              |
| ---- | --------------- |
| 自连接  | 禁止              |
| 重复连接 | 禁止同一对节点重复创建相同关系 |
| 无效节点 | 禁止连接已删除节点       |
| 区域连接 | 禁止              |
| 预览模式 | 禁止创建连线          |


### 12.5 连线展示


| 状态    | 规则                            |
| ----- | ----------------------------- |
| 默认    | 灰蓝色折线                         |
| hover | 高亮                            |
| 选中    | 展示连接关系弹窗，展示两个节点名称与删除连接、关闭按钮   |
| 删除连接  | 选中后 二次确认提示：确定要删除这条连接关系吗？取消/确定 |


---

## 13. 预览模式

### 13.1 进入预览

点击“预览大屏”进入预览模式。

### 13.2 预览模式展示规则


| 内容      | 规则  |
| ------- | --- |
| 左侧节点管理  | 隐藏  |
| 顶部编辑按钮  | 隐藏  |
| 新增区域    | 隐藏  |
| 保存草稿    | 隐藏  |
| 画布编辑锚点  | 隐藏  |
| 区域编辑按钮  | 隐藏  |
| 节点编辑按钮  | 隐藏  |
| 拖拽能力    | 禁止  |
| 缩放 / 平移 | 保留  |
| 节点查看详情  | 隐藏  |


### 13.3 预览顶部按钮


| 按钮   | 行为       |
| ---- | -------- |
| 退出预览 | 回到编辑模式   |
| 发布配置 | 打开发布确认弹窗 |


如果从“有未保存修改”状态进入预览，预览内容应使用当前画布的实时内容。

---

## 14. 保存草稿规则

### 14.1 触发

点击“保存草稿”。

### 14.2 保存内容

保存内容包括：

- 节点列表；
- 节点位置；
- 节点图标；
- 节点关联子拓扑；
- 节点所属区域；
- 区域位置；
- 区域尺寸；
- 区域名称；
- 区域颜色；
- 连线关系；
- 画布缩放比例；
- 画布视口位置。

### 14.3 保存成功

保存成功后：

- 系统保存当前拓扑草稿；
- 自动生成一条草稿版本记录；
- 页面状态更新为：`已保存草稿`；
- 右上角 Toast 提示：`草稿已保存，可在版本历史中查看`；

### 14.4 保存失败

提示：`草稿保存失败，请检查网络后重试。`  
页面状态保持：`有未保存修改`。

保存草稿不影响线上正式大屏。

---

## 15. 发布配置规则

### 15.1 触发

点击“发布配置”。

### 15.2 发布确认弹窗

标题：

```text
确认发布配置？
```

内容：

```text
发布后，当前草稿将覆盖线上大屏配置，并生成一条发布版本记录。是否确认发布？
```

按钮：取消、确认发布。

### 15.3 发布前校验


| 校验项     | 规则             |
| ------- | -------------- |
| 节点名称    | 不允许为空          |
| 节点图标    | 必须选择           |
| 关联子拓扑节点 | 必须选择           |
| 重复连线    | 不允许            |
| 区域名称    | 不允许为空          |
| 重名对象    | 节点名称、区域名称建议不重复 |


### 15.4 发布成功

发布成功后：

- 系统生成一条发布版本记录；
- 新发布版本标记为：`当前生效`；
- 原当前生效版本取消 `当前生效` 标记，转为历史发布版本；
- 页面状态更新为：`已发布`；
- 右上角 Toast 提示：`发布成功，已生成发布版本`；

### 15.5 发布失败

提示：`配置发布失败，请稍后重试。`

如果是校验失败，展示问题列表：

```text
发布失败，存在以下配置问题：
1. 连线“边界防火墙 - 核心路由器”目标节点不存在
2. 区域“核心网络安全域”名称为空
```

发布配置才会影响正式大屏。

---

## 16. 版本历史规则

### 16.1 功能定位

版本历史用于记录用户在当前拓扑配置页中的关键保存与发布行为，包括：

- 点击“保存草稿”后生成的草稿版本；
- 点击“发布配置”并发布成功后生成的发布版本。

版本历史的核心作用是：

```text
查看历史配置
预览历史版本
恢复历史版本为当前草稿
识别当前线上生效版本
```

版本历史不用于直接覆盖线上配置。任何历史版本恢复后，都只进入当前草稿，需要用户再次点击“发布配置”后才会影响正式大屏。

### 16.2 入口规则

顶部工具栏新增入口：`版本历史`。

推荐按钮顺序：

```text
撤销  重做  ｜  新增区域   ｜  预览大屏  保存草稿  发布配置 版本历史
```

点击“版本历史”后，从页面右侧打开版本历史抽屉。

抽屉规则：


| 项目     | 规则             |
| ------ | -------------- |
| 打开方式   | 右侧抽屉打开         |
| 是否跳转页面 | 不跳转            |
| 是否遮挡整页 | 不全屏遮挡，仅覆盖右侧区域  |
| 关闭方式   | 点击关闭按钮或抽屉外区域关闭 |
| 关闭后状态  | 回到当前编辑画布       |


### 16.3 抽屉结构

版本历史抽屉结构：

```text
版本历史

查看保存草稿与发布配置生成的历史版本，可恢复历史版本为当前草稿。

筛选：全部 / 草稿版本 / 发布版本

版本列表
- 版本卡片
- 版本卡片
- 版本卡片
```

版本列表按时间倒序排列，最新版本显示在最上方。

### 16.4 版本类型

#### 16.4.1 草稿版本


| 项目     | 规则               |
| ------ | ---------------- |
| 触发方式   | 点击“保存草稿”并保存成功后生成 |
| 版本编号   | D1、D2、D3……       |
| 时间字段   | 保存时间             |
| 类型标签   | 草稿版本             |
| 支持操作   | 查看、恢复为当前草稿       |
| 是否影响线上 | 不影响              |


草稿版本用于记录阶段性编辑内容。

#### 16.4.2 发布版本


| 项目     | 规则                    |
| ------ | --------------------- |
| 触发方式   | 点击“发布配置”并发布成功后生成      |
| 版本编号   | V1、V2、V3……            |
| 时间字段   | 发布时间                  |
| 类型标签   | 发布版本                  |
| 当前生效标识 | 当前线上使用的发布版本展示“当前生效”   |
| 支持操作   | 查看、基于此版本恢复            |
| 是否影响线上 | 只有发布动作影响线上，查看和恢复不影响线上 |


发布版本用于记录正式发布到大屏的配置版本。

### 16.5 版本卡片字段

每条版本记录使用卡片展示。

版本卡片字段：


| 字段   | 说明                      |
| ---- | ----------------------- |
| 版本编号 | 草稿版本使用 D 编号，发布版本使用 V 编号 |
| 版本类型 | 草稿版本 / 发布版本             |
| 当前生效 | 仅当前线上发布版本展示             |
| 操作时间 | 草稿版本展示保存时间；发布版本展示发布时间   |
| 操作人  | 生成该版本的用户                |
| 操作按钮 | 查看预览 / 恢复               |


### 16.6 版本卡片示例

草稿版本示例：

```text
D08  草稿版本
2026-04-30 10:12
操作人：张三

[查看预览] [恢复该版本]
```

发布版本示例：

```text
V12  发布版本  当前生效
2026-04-30 10:30
操作人：张三

[查看预览] [恢复该版本]
```

### 16.7 查看历史版本

用户点击版本卡片中的“查看预览”后，页面进入历史版本预览态。

预览态规则：


| 项目      | 规则              |
| ------- | --------------- |
| 画布内容    | 切换为该历史版本的拓扑内容   |
| 页面状态    | 历史版本预览中         |
| 是否可编辑   | 不可编辑            |
| 是否可拖拽节点 | 不允许             |
| 是否可新增节点 | 不允许             |
| 是否可新增区域 | 不允许             |
| 是否可编辑连线 | 不允许             |
| 是否可保存草稿 | 不允许             |
| 是否可发布配置 | 不允许             |
| 是否可查看详情 | 不允许查看节点、区域、连线详情 |


顶部提示条文案：

```text
正在查看预览历史版本 V12，不可编辑
```

顶部右侧“退出预览”

点击“退出预览”后，返回进入预览前的当前编辑态。

### 16.8 恢复版本

用户点击草稿版本中的“恢复为该版本”后，弹出确认弹窗。

弹窗标题：

```text
恢复为该版本？
```

弹窗内容：

```text
当前存在未保存修改，恢复历史版本后，这些未保存修改将被覆盖。确认继续？
```

按钮：取消、确认。

确认后：

- toast提示“已恢复为所选版本，请确认后保存或发布”
- 当前画布内容替换为恢复版本内容；
- 页面状态变为：`有未保存修改`；
- 线上正式大屏不受影响。

### 16.9 未保存修改冲突

如果当前画布存在未保存修改，用户点击恢复历史版本时，需要二次确认。

警示文案：

```text
当前存在未保存修改，恢复历史版本后，这些未保存修改将被覆盖。
```

按钮：取消、确认覆盖并恢复。

### 16.10 筛选规则


| 筛选项  | 说明                     |
| ---- | ---------------------- |
| 全部   | 展示全部草稿版本和发布版本，按时间倒序排列  |
| 草稿版本 | 只展示草稿版本                |
| 发布版本 | 只展示发布版本，当前线上版本展示“当前生效” |


草稿版本无结果：

```text
暂无草稿版本
点击保存草稿后，将生成草稿版本记录。
```

发布版本无结果：

```text
暂无发布版本
点击发布配置后，将生成发布版本记录。
```

### 16.11 空状态

当没有任何版本记录时，版本历史抽屉展示空状态：

```text
暂无版本记录
保存草稿或发布配置后，将自动生成版本记录。
```

如果当前存在未保存修改，可提供“保存草稿”按钮；如果没有未保存修改，则按钮置灰。

### 16.12 版本历史权限


| 权限    | 规则               |
| ----- | ---------------- |
| 查看权限  | 可查看版本历史、查看历史版本预览 |
| 编辑权限  | 可恢复历史版本为当前草稿     |
| 发布权限  | 可发布恢复后的草稿        |
| 无查看权限 | 隐藏版本历史入口或置灰      |


### 16.13 版本历史边界

```text
保存草稿会生成草稿版本。
发布配置会生成发布版本。
查看历史版本只读，不可编辑。
恢复历史版本只恢复为当前草稿。
恢复历史版本不直接影响线上正式大屏。
当前生效标识只用于发布版本。
不做时间轴回放。
不做版本差异对比。
不做直接回滚线上版本。
```

---

## 17. 撤销与重做

### 17.1 支持操作

支持撤销 / 重做：

- 新增节点；
- 编辑节点；
- 删除节点；
- 移动节点；
- 新增区域；
- 编辑区域；
- 删除区域；
- 移动区域；
- 调整区域尺寸；
- 新增连线；
- 删除连线。

### 17.2 按钮状态


| 状态     | 规则    |
| ------ | ----- |
| 无可撤销操作 | 撤销置灰  |
| 有可撤销操作 | 撤销可点击 |
| 无可重做操作 | 重做置灰  |
| 有可重做操作 | 重做可点击 |


### 17.3 操作规则

- 撤销 / 重做不需要二次确认。
- 删除类操作执行前仍需二次确认。
- 每次撤销 / 重做后，页面状态变为“有未保存修改”。
- 用户执行新的编辑操作后，重做栈清空。

---

## 18. 删除规则

### 18.1 删除节点

触发入口：

- 左侧节点卡片删除；
- 右侧节点详情删除；
- 画布选中节点后按 Delete。

确认文案：

```text
确认删除该节点？
删除后，该节点将从当前拓扑草稿中移除，与该节点相关的连线也会被删除。
如果该节点关联了子拓扑，只会解除当前节点与子拓扑的入口关系，不会删除目标子拓扑。
```

删除后：

- 节点从画布移除；
- 左侧列表同步移除；
- 相关连线同步删除；
- 页面进入未保存状态；
- 可通过撤销恢复。

### 18.2 删除区域

确认文案：

```text
当区域内无节点时：
提示为：确认删除该区域？

当有区域内最少有一个节点时：
提示为：当前区域内存在节点，请先迁移或删除区域内对象。）
```

删除后：

- 区域移除；
- 页面进入未保存状态。

### 18.3 删除连线

确认文案：

```text
确认删除该连线？
删除后，该连线关系将从当前草稿中移除。
```

删除后：

- 连线移除；
- 节点保留；
- 页面进入未保存状态。

---

## 19. 未保存离开提示

当状态为“有未保存修改”时，用户执行以下行为需要弹出确认：

- 切换左侧菜单；
- 返回上一页；
- 刷新页面；
- 关闭页面；
- 跳转到其他模块；
- 点击顶部全局导航。

弹窗标题：

```text
存在未保存修改
```

弹窗内容：

```text
当前拓扑配置存在未保存修改，离开后修改将丢失。请选择是否保存草稿。
```

按钮：放弃修改、留在当前页、保存草稿。


| 按钮    | 行为         |
| ----- | ---------- |
| 放弃修改  | 丢弃修改并离开    |
| 留在当前页 | 关闭弹窗，停留当前页 |
| 保存草稿  | 保存成功后继续离开  |


---

## 20. 空状态、加载状态、错误状态

### 20.1 画布空状态

```text
暂无拓扑内容
可先新增区域，或新增节点开始配置。
```

按钮：新增区域、新增节点。

### 20.2 节点列表空状态

```text
暂无节点
点击“新增节点”创建拓扑节点
```

### 20.3 搜索无结果

```text
未找到匹配节点
请尝试更换关键词
```

### 20.4 加载状态


| 场景       | 展示                        |
| -------- | ------------------------- |
| 页面加载     | 骨架屏 / loading             |
| 保存草稿中    | 保存按钮 loading              |
| 发布配置中    | 发布按钮 loading              |
| 子拓扑列表加载中 | 下拉 / 选择弹窗 loading         |
| 版本历史加载中  | 版本历史抽屉 skeleton / loading |


### 20.5 错误状态


| 场景          | 文案                 |
| ----------- | ------------------ |
| 页面加载失败      | 拓扑配置加载失败，请刷新页面重试   |
| 保存失败        | 草稿保存失败，请检查网络后重试    |
| 发布失败        | 配置发布失败，请稍后重试       |
| 子拓扑不存在      | 关联子拓扑不存在或无权限访问     |
| 子拓扑设备明细加载失败 | 子拓扑下关联设备加载失败，请稍后重试 |
| 版本历史加载失败    | 版本历史加载失败，请稍后重试     |


---

## 21. 权限规则


| 权限       | 可执行操作          |
| -------- | -------------- |
| 查看权限     | 只能查看和预览，不可编辑   |
| 版本历史查看权限 | 可查看版本历史和历史版本预览 |
| 编辑权限     | 可新增、编辑、删除、保存草稿 |
| 发布权限     | 可发布配置          |
| 管理员      | 拥有全部权限         |


无权限时：


| 场景         | 规则              |
| ---------- | --------------- |
| 无编辑权限      | 隐藏新增、编辑、删除、保存草稿 |
| 无发布权限      | 发布按钮置灰          |
| 无版本历史查看权限  | 隐藏版本历史入口或置灰     |
| 无子拓扑访问权限   | 点击进入时提示无权限      |
| 无子拓扑设备明细权限 | 隐藏设备列表或展示无权限提示  |


---

## 22. 数据结构建议

### 22.1 Node

```ts
type TopologyNode = {
  id: string;
  name: string;
  iconType:
    | 'firewall'
    | 'router'
    | 'switch'
    | 'server'
    | 'terminal'
    | 'middleware'
    | 'database'
    | 'gateway'
    | 'cloud'
    | 'group'
    | 'custom';

  // 可选：节点作为子拓扑入口时才存在
  linkedTopologyId?: string;
  linkedTopologyName?: string;
  linkedTopologyDeviceCount?: number;

  regionId?: string;
  description?: string;

  position: {
    x: number;
    y: number;
  };

  updatedAt?: string;
};
```

### 22.2 NodeDetailResponse

```ts
type NodeDetailResponse = {
  node: TopologyNode;

  // 当节点关联子拓扑时返回，用于右侧详情展示
  linkedTopologyDevices?: Array<{
    id: string;
    name: string;
    ip?: string;
    deviceType?: string;
  }>;
};
```

### 22.3 Region

```ts
type TopologyRegion = {
  id: string;
  name: string;
  color: 'blue' | 'green' | 'orange' | 'purple' | 'custom';
  description?: string;
  position: {
    x: number;
    y: number;
  };
  size: {
    width: number;
    height: number;
  };
};
```

### 22.4 Edge

```ts
type TopologyEdge = {
  id: string;
  sourceNodeId: string;
  targetNodeId: string;
  label?: string;
  description?: string;
};
```

### 22.5 Version

```ts
type TopologyVersion = {
  id: string;
  versionNo: string; // D1 / D2 / V1 / V2
  versionType: 'draft' | 'published';
  isCurrentPublished?: boolean;
  topologySnapshot: {
    nodes: TopologyNode[];
    regions: TopologyRegion[];
    edges: TopologyEdge[];
    viewport?: {
      zoom: number;
      x: number;
      y: number;
    };
  };
  operatorId: string;
  operatorName: string;
  createdAt: string;
};
```

## 23. 需要明确移除的旧逻辑

最终版本中，以下旧逻辑必须删除：

1. 删除“设备 / 集群”Tab。
2. 删除左侧“资源库”作为主结构。
3. 删除“新建设备 / 新建集群”两个主入口。
4. 删除“未加入集群”分组。
5. 删除“集群展开查看设备列表”的资源库逻辑。
6. 删除“设备拖入集群”的交互。
7. 删除“设备加入集群后不再独立展示”的规则。
8. 删除“节点直接关联物理设备”的选项。
9. 删除“关联对象类型：不关联 / 关联设备 / 关联子拓扑”的切换控件。
10. 删除“查看设备信息”按钮。
11. 删除节点状态点、CPU、内存等运行态信息展示。
12. 删除“子拓扑作为独立对象添加到画布”的入口。
13. 删除“进入内部拓扑只绑定集群”的规则。
14. 删除“区域必须承载设备 / 集群”的强绑定规则。
15. 删除“区域随内部节点自动缩放”的逻辑。
16. 删除连线方向、流量、链路状态等复杂字段，第一版只表达关系。

## 24. 最终规则边界

### 24.1 节点边界

```text
节点是画布对象。
设备不是节点新增时的直接关联对象。
集群不是独立资源，集群只是节点的一种图标或展示方式。
子拓扑不是画布对象，只能通过节点关联进入。
节点可以不关联子拓扑，作为普通画布节点存在。
```

### 24.2 子拓扑边界

```text
子拓扑不能单独添加到画布。
子拓扑只能从节点中选择并关联。
一个节点最多关联一个子拓扑。
已关联子拓扑的节点可作为下钻入口。
查看节点详情时，展示该子拓扑下的关联设备列表。
```

### 24.3 区域边界

```text
区域是视觉分组，不是强业务容器。
节点可以不属于任何区域。
区域删除不删除节点。
区域大小由用户手动控制。
区域不会随内部节点移动自动改变大小。
```

### 24.4 连线边界

```text
连线只连接节点。
连线不连接区域。
连线不表达方向。
连线不表达状态。
连线不表达流量。
连线不表达链路质量。
```

### 24.5 保存发布边界

```text
编辑只影响草稿。
保存草稿只保存编辑进度。
发布配置才影响正式大屏。
未保存修改离开页面必须提示。
删除类操作必须二次确认。
```

### 24.6 版本历史边界

```text
保存草稿生成草稿版本。
发布配置生成发布版本。
当前生效只标记发布版本。
查看历史版本为只读预览，不允许编辑。
恢复历史版本只恢复为当前草稿，不直接影响线上。
恢复后需要再次发布才会影响正式大屏。
```

## 25. 验收标准

### 25.1 概念验收

- 页面中不再出现“设备 / 集群 Tab”。
- 页面中不再出现“资源库”主结构。
- 页面统一使用“节点管理”。
- 新增统一为“新增节点”。
- 设备类型、集群语义都通过节点图标表达。
- 新增 / 编辑节点中不出现“关联设备”选项。
- 新增 / 编辑节点中不出现“关联对象类型：不关联 / 关联设备 / 关联子拓扑”的切换。
- 子拓扑不能单独添加，只能通过节点关联。

### 25.2 左侧面板验收

- 左侧标题为“节点管理”。
- 左侧有“新增节点”按钮。
- 左侧展示当前拓扑已有节点。
- 支持搜索节点名称，可扩展搜索子拓扑名称、区域名称、图标类型。
- 支持节点定位、编辑、删除。
- 不展示关联设备名称或关联设备 IP。

### 25.3 画布验收

- 支持区域、节点、连线展示。
- 节点支持拖拽。
- 区域支持拖拽和缩放。
- 区域不随节点移动自动变化大小。
- 连线只连接节点。
- 删除节点时相关连线同步删除。
- 点击不同对象，右侧面板正确切换。

### 25.4 节点验收

- 节点必须有图标和显示名称。
- 节点可选关联一个子拓扑，也可以不关联子拓扑。
- 节点不能直接关联物理设备。
- 已关联子拓扑节点可进入子拓扑。
- 已关联子拓扑节点详情中展示该子拓扑下的关联设备列表。
- 选择“群组 / 集群”图标时，仍然只是节点，不是独立集群资源。
- 节点不展示设备状态点、CPU、内存等运行态信息。

### 25.5 保存发布验收

- 进入页面默认可编辑。
- 编辑操作进入草稿态。
- 保存草稿不影响线上。
- 发布配置需要二次确认。
- 发布成功后状态变为“已发布”。
- 有未保存修改时离开页面必须提示。

### 25.6 版本历史验收

- 顶部工具栏存在“版本历史”入口。
- 点击“版本历史”后，从右侧打开版本历史抽屉。
- 版本历史支持“全部 / 草稿版本 / 发布版本”筛选。
- 点击保存草稿后，自动生成草稿版本记录。
- 点击发布配置并发布成功后，自动生成发布版本记录。
- 当前线上生效的发布版本显示“当前生效”。
- 点击“查看”后进入历史版本只读预览态。
- 历史版本预览态不允许新增、编辑、拖拽、保存、发布。
- 草稿版本支持“恢复为当前草稿”。
- 发布版本支持“基于此版本恢复”。
- 恢复历史版本不会直接影响线上正式大屏。
- 当前存在未保存修改时，恢复历史版本必须二次确认。
- 无版本记录时展示版本历史空状态。

